[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    unit: Unit tests (no external services)
    integration: Integration tests (API with mocks)
    e2e: End-to-end tests (full stack, optional in CI)
